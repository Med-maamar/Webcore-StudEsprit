# ──────────────────────────────────────────────────────────────
# Core web framework & DB
Django>=5.0,<6.0
django-environ>=0.12.0
python-dotenv>=1.1.1
pymongo>=4.15.2
mongoengine==0.29.1
djangorestframework>=3.15.2,<3.16
django-rest-framework-mongoengine==3.4.1
django-htmx>=1.26.0

# ──────────────────────────────────────────────────────────────
# HTTP / API
requests>=2.32.0
openai>=2.6.1
google-generativeai>=0.8.5
google-auth>=2.41.1

# ──────────────────────────────────────────────────────────────
# Security / utils
argon2-cffi>=25.1.0
python-slugify>=8.0.4
ruff>=0.13.3
black>=25.9.0

# ──────────────────────────────────────────────────────────────
# Flask (you have both Django & Flask – that’s fine)
Flask>=3.0.0,<3.1
gunicorn>=21.2.0
flask-cors>=3.0.11

# ──────────────────────────────────────────────────────────────
# Data / ML
pandas>=2.3.0
numpy>=1.26.4
scikit-learn>=1.3.2
PyMuPDF>=1.25.0
sentence-transformers>=2.2.2
mongomock>=4.1.2

# ──────────────────────────────────────────────────────────────
# **CPU-ONLY PyTorch** – MUST BE AT THE VERY END
# (torch pulls in torchvision/torchaudio automatically via sentence-transformers)
-f https://download.pytorch.org/whl/cpu/torch_stable.html
torch==2.9.0+cpu
torchvision==0.20.0+cpu
torchaudio==2.9.0+cpu
# ──────────────────────────────────────────────────────────────
